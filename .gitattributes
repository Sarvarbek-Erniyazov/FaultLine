* text=auto eol=lf
*.png binary
*.jpg binary
*.zip binary
*.parquet binary
*.xlsx binary
*.ipynb -text

# Third-party data excerpts are committed byte for byte. Line-ending normalisation
# would make them differ from what the provider published, and the README beside them
# claims they are verbatim -- so the claim has to stay true on every checkout.
tests/fixtures/telemetry/** -text
